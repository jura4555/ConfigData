spring:
  application:
    name: storage-service
  datasource:
    url: ${STORAGE_SERVICE_DB_URL:jdbc:postgresql://localhost:5432/storagedb}
    username: ${STORAGE_SERVICE_DB_USER:postgres}
    password: ${STORAGE_SERVICE_DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate.format_sql: true

server:
  port: ${STORAGE_SERVICE_INSIDE_PORT:8084}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_CLIENT_SERVICE_URL_DEFAULT_ZONE:http://localhost:8761/eureka/}
    registerWithEureka: true
    fetchRegistry: true
    enabled: true
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  health:
    show-details: always
  metrics:
    export:
      prometheus:
        enabled: true

storage:
  paths:
    STAGING: staging/
    PERMANENT: permanent/

logging:
  logstash:
    host: ${LOGSTASH_HOST:localhost}
    port: ${LOGSTASH_PORT:5000}







